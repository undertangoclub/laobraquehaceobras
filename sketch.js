function setup() {
    // Este es un buen lugar para inicializar el tamaño del lienzo, cargar imágenes, etc.
    createCanvas(windowWidth, windowHeight);
  }
  
  function draw() {
    // Este es un buen lugar para dibujar formas, actualizar variables, manejar interacciones, etc.
    background(220);
  }
  